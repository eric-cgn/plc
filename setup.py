from setuptools import setup, find_packages

setup(
    name='plc',
    version='1.0.0',
    author='Eric McIntyre',
    description='Basic interface class for PowerLinc v2 USB controller.',
    packages=find_packages(),    
    install_requires=[],
)
